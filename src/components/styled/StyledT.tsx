import { T } from '@admiral-ds/react-ui'
import styled from 'styled-components'
import { spacer, SpacingAttr } from '../../helpers/spacer'

const StyledT = styled(T) <SpacingAttr>`
  ${spacer};
`

export default StyledT;